import 'package:flutter/cupertino.dart';

class Categoriescreen extends StatelessWidget {
  const Categoriescreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('categories screen');
  }
}
